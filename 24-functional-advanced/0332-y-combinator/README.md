# 0332 — Y Combinator

Define factorial via a fixed-point combinator (no named self-recursion) and compute 5!, printing `120`. A self-applying `Rec` interface yields a `fix` combinator applied to a non-recursive factorial generator.

## Run

    javac YCombinator.java && java YCombinator
