# 0006 — Functions

Define `add(a, b)` returning their sum, call it with `3` and `4`, and print
the result. In Java, functions are **methods** that live inside a class.
`add` is declared `static` so that `main` (also static) can call it
directly without first creating an instance of `Functions`.

## Run

    javac Functions.java && java Functions
