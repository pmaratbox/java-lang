# 0665 — Big integer addition

Add two huge integers with Java's `java.math.BigInteger`, an arbitrary-precision integer type. Each value exceeds the range of `long`, but `BigInteger.add` returns the exact sum of `12345678901234567890 + 98765432109876543210`.

## Run

    jbang BignumAdd.java
