# 0264 — Turnstile FSM

Drive a turnstile (locked/unlocked) with events coin, push, push and print the resulting states `unlocked locked locked`. A small step method encodes the transition rules.

## Run

    javac TurnstileFsm.java && java TurnstileFsm
