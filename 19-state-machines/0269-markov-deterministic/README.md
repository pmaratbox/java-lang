# 0269 — Deterministic Transition Walk

Walk a deterministic transition map A->B->C->A for three steps from A, printing the visited states `B C A`. A HashMap is the natural next-state lookup.

## Run

    javac MarkovDeterministic.java && java MarkovDeterministic
