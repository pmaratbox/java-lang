# 0268 — Cellular Automaton Rule 90

Apply one Rule 90 step (new = left XOR right) to the row "00100" with zero boundaries, printing `01010`. Java's `^` operator gives the XOR directly.

## Run

    javac Rule90Step.java && java Rule90Step
