# 0273 — KMP Search

Use Knuth-Morris-Pratt to find all start indices of "ab" in "ababab", printing `0 2 4`. Java builds the LPS array with primitive int[] and scans in a single pass.

## Run

    javac KmpSearch.java && java KmpSearch
