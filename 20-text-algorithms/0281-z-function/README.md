# 0281 — Z-Function

Compute the Z-array of "aaaa" (excluding z[0]) and print `3 2 1`. Java computes the Z-array with the classic l/r window over a primitive int[].

## Run

    javac ZFunction.java && java ZFunction
