# 0278 — Suffix Array

Build the suffix array of "banana" (sorted suffix start indices), printing `5 3 1 0 4 2`. Java sorts an Integer[] of indices with a Comparator keyed on String::substring.

## Run

    javac SuffixArray.java && java SuffixArray
