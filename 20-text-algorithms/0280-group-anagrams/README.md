# 0280 — Group Anagrams

Group ["eat","tea","tan","ate","nat"] into anagram classes and print the number of groups `2`. Java sorts each word's char[] into a canonical key collected in a HashSet.

## Run

    javac GroupAnagrams.java && java GroupAnagrams
