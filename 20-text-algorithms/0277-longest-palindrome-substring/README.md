# 0277 — Longest Palindromic Substring

Find the longest palindromic substring of "babad" (first of maximal length), printing `bab`. Java expands around each odd and even center, keeping the earliest maximal window.

## Run

    javac LongestPalindromeSubstring.java && java LongestPalindromeSubstring
