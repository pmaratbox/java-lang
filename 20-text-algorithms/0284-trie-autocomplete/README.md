# 0284 — Trie Autocomplete

Insert "car","card","dog" into a trie and autocomplete the prefix "car", printing `car card`. Java builds the trie from TreeMap children so a DFS yields words in lexicographic order.

## Run

    javac TrieAutocomplete.java && java TrieAutocomplete
