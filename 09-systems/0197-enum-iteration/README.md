# 0197 — Enum Iteration

Iterate over all values of a Color enum (RED, GREEN, BLUE) and print their names `RED GREEN BLUE`. Java's `Enum.values()` returns the constants in declaration order, and `name()` gives each constant's name.

## Run

    javac Iteration.java && java Iteration
