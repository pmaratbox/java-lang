# 0199 — Struct Field Names

List the field names of a Point{x,y} struct/record and print `x y`. Java records expose their components via reflection through `getRecordComponents()`, preserving declaration order.

## Run

    javac FieldNames.java && java FieldNames
