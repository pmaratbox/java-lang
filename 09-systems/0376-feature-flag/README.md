# 0376 — Feature Flag

Branch on a boolean feature flag: on -> "enabled", off -> "disabled", printing `enabled disabled`. A ternary expression selects the message from the flag.

## Run

    javac FeatureFlag.java && java FeatureFlag
