# 0377 — Permission Bits

Decode the Unix permission bits 0b101 into the rwx string `r-x`. Bitwise masks against `0b100/0b010/0b001` test each permission bit.

## Run

    javac PermissionBits.java && java PermissionBits
