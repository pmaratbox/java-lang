# 0194 — Try / Catch / Finally

Throw and catch an error, printing `caught`, and always run a finally block printing `cleanup`, on two lines. Java's `try` / `catch` / `finally` guarantees the `finally` block runs whether or not an exception was thrown.

## Run

    javac CatchFinally.java && java CatchFinally
