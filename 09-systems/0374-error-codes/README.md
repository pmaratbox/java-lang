# 0374 — Error Codes

Map the numeric error code 2 to its message "not found" and print `not found`. A `Map<Integer, String>` gives a direct code-to-message lookup.

## Run

    javac ErrorCodes.java && java ErrorCodes
