# 0285 — Coin Change (Min Coins)

Find the fewest coins from {1,2,5} summing to 11, printing `3`. Java fills the DP table with `Arrays.fill` as a sentinel.

## Run

    javac CoinChangeMin.java && java CoinChangeMin
