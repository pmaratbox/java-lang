# 0295 — Equal Partition

Decide whether [1,5,11,5] can split into two equal-sum subsets, printing `yes`. Java reduces to a subset-sum for half the total.

## Run

    javac PartitionEqualSubset.java && java PartitionEqualSubset
