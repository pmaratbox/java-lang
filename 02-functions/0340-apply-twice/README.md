# 0340 — Apply Twice

Write applyTwice(f,x)=f(f(x)) and apply it with inc to 3, printing `5`. In Java, `Function<T,T>` models the higher-order parameter cleanly.

## Run

    javac ApplyTwice.java && java ApplyTwice
