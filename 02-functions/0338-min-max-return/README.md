# 0338 — Return Min and Max

Return both the minimum and maximum of [4,1,7] from one function and print `1 7`. In Java, a record gives a concise immutable pair to return both values.

## Run

    javac MinMaxReturn.java && java MinMaxReturn
