# 0339 — Recursive List Sum

Recursively sum the list [1,2,3,4], printing `10`. In Java, `List.subList` gives the tail without copying for the recursive step.

## Run

    javac RecursiveSum.java && java RecursiveSum
