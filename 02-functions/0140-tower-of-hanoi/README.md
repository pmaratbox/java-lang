# 0140 — Tower of Hanoi

Count the moves needed to solve Tower of Hanoi for 3 disks recursively and print `7`. A static recursive method doubles the subproblem count and adds one for each level.

## Run

    javac OfHanoi.java && java OfHanoi
