# 0141 — Ackermann Function

Compute the Ackermann function A(2,3) recursively and print `9`. The classic two-argument definition maps directly onto nested static method calls in Java.

## Run

    javac Ackermann.java && java Ackermann
