# 0341 — Recursive List Reverse

Reverse [1,2,3] using recursion (no built-in reverse), printing `3 2 1`. In Java, recurse on the tail sublist and append the head to build the reversed list.

## Run

    javac RecursiveListReverse.java && java RecursiveListReverse
