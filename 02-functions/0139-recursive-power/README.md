# 0139 — Recursive Power

Compute 2 raised to the 10th power recursively and print `1024`. Java resolves the recursion on the call stack with a simple static helper method.

## Run

    javac Power.java && java Power
