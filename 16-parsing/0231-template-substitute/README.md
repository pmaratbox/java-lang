# 0231 — Template Substitution

Substitute the variable in the template "hi {name}" with name="Ada", printing `hi Ada`. Java replaces each `{key}` via a regex `Matcher` backed by a map.

## Run

    javac TemplateSubstitute.java && java TemplateSubstitute
