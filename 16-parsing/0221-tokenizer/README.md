# 0221 — Tokenizer

Tokenize the expression "1 + 2" into token types, printing `NUM PLUS NUM`. Java scans characters with `Character.isDigit` and emits token-type names.

## Run

    javac Tokenizer.java && java Tokenizer
