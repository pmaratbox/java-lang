# 0234 — Parse Number Bases

Parse "ff" as hexadecimal (255) and "101" as binary (5), printing `255 5`. Java uses `Integer.parseInt` with an explicit radix.

## Run

    javac NumberBaseParse.java && java NumberBaseParse
