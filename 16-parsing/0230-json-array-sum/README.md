# 0230 — Parse JSON Array

Parse the JSON array text "[1,2,3]" and print the sum of its elements `6`. Java strips the brackets, splits on commas, parses ints, and sums.

## Run

    javac JsonArraySum.java && java JsonArraySum
