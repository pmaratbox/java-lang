# 0226 — Roman to Integer

Parse the Roman numeral "XIV" to its value `14`. Java sums values left-to-right, subtracting when a smaller numeral precedes a larger.

## Run

    javac RomanToInt.java && java RomanToInt
