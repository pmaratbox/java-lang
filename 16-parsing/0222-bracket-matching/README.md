# 0222 — Bracket Matching

Check that mixed brackets are balanced: "([{}])" (yes) and "([)]" (no), printing `yes no`. Java uses an `ArrayDeque` as a stack to push opens and match closes.

## Run

    javac BracketMatching.java && java BracketMatching
