# 0232 — Word/Line/Char Counts

For the text "a b\nc" report words, lines, and characters, printing `3 2 5`. Java counts words by splitting on whitespace, lines by newlines plus one, and chars by length.

## Run

    javac WcCounts.java && java WcCounts
