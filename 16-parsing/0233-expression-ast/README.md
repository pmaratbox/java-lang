# 0233 — Expression AST

Build an AST for "1+2*3" and evaluate it to `7`. Java models nodes as a `Node` interface with `Add`/`Mul`/`Num` classes and a recursive `eval`.

## Run

    javac ExpressionAst.java && java ExpressionAst
