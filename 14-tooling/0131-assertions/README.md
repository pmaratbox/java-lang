# 0131 — Assertions

Assert several expected equalities (all true) and print `all passed`. A tiny helper throws `AssertionError` when a condition is false.

## Run

    javac Assertions.java && java Assertions
