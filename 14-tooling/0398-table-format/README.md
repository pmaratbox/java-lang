# 0398 — Aligned Table

Format the rows (a,1),(bb,22) with the first column left-padded to the widest value, printing two aligned rows. String.format with a computed `%-Ns` width left-justifies each first cell idiomatically in Java.

## Run

    javac TableFormat.java && java TableFormat
