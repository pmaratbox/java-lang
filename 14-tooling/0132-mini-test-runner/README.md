# 0132 — Mini Test Runner

Run three named test functions that all pass and report `3 passed, 0 failed`. A `LinkedHashMap` of named `Supplier<Boolean>` closures is run in order and tallied.

## Run

    javac TestRunner.java && java TestRunner
