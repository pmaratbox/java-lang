# 0400 — ASCII Histogram

Render a histogram of [3,1,2] as rows of "#" characters, one bar per line. String.repeat renders each bar in one expressive line idiomatically in Java.

## Run

    javac AsciiHistogram.java && java AsciiHistogram
