# 0133 — Debug Representation

Produce a debug/structured representation of a Point(1,2) and print `Point(x=1, y=2)`. A `record` with a custom `toString` yields the structured form.

## Run

    javac Repr.java && java Repr
