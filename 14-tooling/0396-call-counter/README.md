# 0396 — Call Counter

Wrap a function so each call increments a counter; after five calls print `calls: 5`. A Runnable closing over a mutable holder counts each invocation idiomatically in Java.

## Run

    javac CallCounter.java && java CallCounter
