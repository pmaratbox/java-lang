# 0525 — Weekday

Get the ISO weekday number of the fixed date `2026-06-15` using `java.time`. `LocalDate.getDayOfWeek().getValue()` already follows ISO numbering (Monday=1 .. Sunday=7), so the Monday date prints `1`.

## Run

    jbang Weekday.java
