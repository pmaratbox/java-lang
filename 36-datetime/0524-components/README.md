# 0524 — Date components

Java's `java.time.LocalDate` parses the fixed ISO date `2026-06-15` and its accessors `getYear()`, `getMonthValue()`, and `getDayOfMonth()` extract each component, printed on its own line.

## Run

    jbang Components.java
