# 0522 — Add days

Java's `java.time` library parses the fixed date `2026-06-15` with `LocalDate.parse`, performs date arithmetic with `LocalDate.plusDays(10)`, and prints the result in ISO-8601 format (`yyyy-MM-dd`) via the default `toString()`.

## Run

    jbang AddDays.java
