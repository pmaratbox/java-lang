# 0312 — Tree Height

Compute the height (in nodes) of the BST built from 5,3,8,1,4, printing `3`. `Math.max` over the two recursive heights is the idiomatic Java form.

## Run

    javac TreeHeight.java && java TreeHeight
