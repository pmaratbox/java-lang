# 0313 — Level-Order Traversal

Print the breadth-first (level-order) traversal of the BST built from 5,3,8,1,4 `5 3 8 1 4`. An `ArrayDeque` is Java's go-to FIFO queue for BFS.

## Run

    javac LevelOrderTraversal.java && java LevelOrderTraversal
