# 0319 — Segment Tree Range Sum

Build a segment tree over [1,2,3,4,5] and query the sum of indices 1..3 (inclusive), printing `9`. The iterative bottom-up `int[]` layout keeps the Java segment tree compact.

## Run

    javac SegmentTreeSum.java && java SegmentTreeSum
