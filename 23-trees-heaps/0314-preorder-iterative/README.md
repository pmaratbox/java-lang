# 0314 — Iterative Preorder

Print the iterative (stack-based) preorder traversal of the BST built from 5,3,8,1,4 `5 3 1 4 8`. `ArrayDeque` doubles as Java's stack via `push`/`pop`.

## Run

    javac PreorderIterative.java && java PreorderIterative
