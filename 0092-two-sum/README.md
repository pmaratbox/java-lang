# 0092 — Two Sum

Find the two indices in `2, 7, 11, 15` whose values sum to `9` and print them: `0 1`. A `HashMap` of value to index returns the complement's earlier index (or `null`), giving a single-pass solution.

## Run

    javac TwoSum.java && java TwoSum
