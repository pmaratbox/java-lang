# 0304 — Connected Components

Count the connected components of the undirected graph with edges (0,1),(1,2),(3,4) over nodes 0..4, printing `2`. Union-find merges edges and a `HashSet` of roots counts the groups.

## Run

    javac ConnectedComponents.java && java ConnectedComponents
