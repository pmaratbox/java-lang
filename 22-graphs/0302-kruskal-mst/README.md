# 0302 — Kruskal MST

Compute the MST total weight of edges (0,1,1),(1,2,2),(0,2,3) with union-find, printing `3`. Edges are sorted by `Arrays.sort` with a comparator, then merged with path-compressed find.

## Run

    javac KruskalMst.java && java KruskalMst
