# 0711 — Parse and format

This lesson uses the JDK's built-in `java.util.UUID` library to parse the uppercase string `550E8400-E29B-41D4-A716-446655440000` via `UUID.fromString`, then prints its canonical lowercase form with `toString()`.

## Run

    jbang UuidRoundtrip.java
