# 0394 — Add Minutes to Time

Add 90 minutes to 10:45 and print the resulting 24-hour time `12:15`. Java's `LocalTime.plusMinutes` handles the carry, formatted as `HH:mm`.

## Run

    javac TimeAddMinutes.java && java TimeAddMinutes
