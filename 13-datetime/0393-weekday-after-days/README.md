# 0393 — Weekday After N Days

Given 2000-01-01 is a Saturday, compute the weekday 3 days later, printing `Tuesday`. Java's `LocalDate.plusDays` plus `DayOfWeek.getDisplayName` yields the full name.

## Run

    javac WeekdayAfterDays.java && java WeekdayAfterDays
