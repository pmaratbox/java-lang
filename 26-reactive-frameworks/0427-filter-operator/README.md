# 0427 — Filter Operator

Use the library's filter operator to keep the even values of 1..6. Built with Project Reactor (reactor-core) using Flux.range and the filter operator.

## Run

    jbang FilterOperator.java
