# 0425 — Create a Stream

Create a stream of 1, 2, 3 with your language's reactive library and print each value, then print done on completion. Uses Project Reactor's `Flux.just` creation operator with an `onNext`/`onComplete` subscriber.

## Run

    jbang ObservableCreate.java
