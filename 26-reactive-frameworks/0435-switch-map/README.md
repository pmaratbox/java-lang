# 0435 — SwitchMap

Use the library's switchMap on a virtual/test scheduler so a new outer value cancels the previous inner stream. Built with Project Reactor's `switchMap` driven by `VirtualTimeScheduler`.

## Run

    jbang SwitchMap.java
