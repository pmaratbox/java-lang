# 0061 — Reverse a String

Reverse the string `abc` character by character and print the result: `cba`. `StringBuilder.reverse()` reverses the characters and is surrogate-aware — it keeps UTF-16 surrogate pairs (e.g. emoji) together.

## Run

    javac ReverseString.java && java ReverseString
