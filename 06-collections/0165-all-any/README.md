# 0165 — All and Any

For [2,4,6] report whether all are even (yes) and whether any are odd (no), printing `yes no`. Java streams expose `allMatch` and `anyMatch` for these quantifiers.

## Run

    javac Any.java && java Any
