# 0083 — Sliding Window

Slide a window of size `2` over `1, 2, 3, 4` and print each window on its own line: `1 2`, `2 3`, `3 4`. A start index walks while the window fits; the inner loop builds each window's line.

## Run

    javac SlidingWindow.java && java SlidingWindow
