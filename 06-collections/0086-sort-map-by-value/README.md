# 0086 — Sort a Map by Value

Sort the map `{a: 3, b: 1, c: 2}` by value in ascending order and print the entries: `b:1 c:2 a:3`. `Map.Entry.comparingByValue()` provides a ready comparator for sorting the entry list by value.

## Run

    javac SortMapByValue.java && java SortMapByValue
