# 0170 — Index By Key

Index people [(1,alice),(2,bob)] by id into a map, look up id 2, and print `id 2: bob`. A `record` models each person while a `Map` indexes them by id.

## Run

    javac ByKey.java && java ByKey
