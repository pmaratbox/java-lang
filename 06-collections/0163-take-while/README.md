# 0163 — Take While

From [1,2,3,4,1] take elements while they are < 3, printing `1 2`. Java's `Stream.takeWhile` stops at the first element failing the predicate.

## Run

    javac While.java && java While
