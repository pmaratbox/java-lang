# 0167 — Top-K Frequent

Find the 2 most frequent elements of [a,b,a,c,b,a], printing `a b`. A `LinkedHashMap` of counts via `merge` keeps a stable order, then a descending sort by count takes the top two.

## Run

    javac KFrequent.java && java KFrequent
