# 0360 — Interleave Lists

Interleave [1,3,5] and [2,4,6] element by element, printing `1 2 3 4 5 6`. One index walks both `List`s in lockstep, adding one element from each into a `StringJoiner`.

## Run

    javac InterleaveLists.java && java InterleaveLists
