# 0085 — Rotate a List

Rotate the list `1, 2, 3, 4, 5` left by `2` positions (elements wrap to the end) and print it: `3 4 5 1 2`. `Collections.rotate` rotates right for positive distances, so `-2` rotates left by two.

## Run

    javac RotateList.java && java RotateList
