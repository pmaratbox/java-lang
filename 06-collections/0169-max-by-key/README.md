# 0169 — Max By Key

From ["a","bbb","cc"] pick the longest string, printing `bbb`. `Stream.max` with a `Comparator.comparingInt` on length gives the argmax.

## Run

    javac ByKey.java && java ByKey
