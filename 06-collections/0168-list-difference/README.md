# 0168 — List Difference

Compute [1,2,3,4] minus the elements of [2,4], printing `1 3`. Filtering the first list against a `Set` of removed elements keeps original order.

## Run

    javac Difference.java && java Difference
