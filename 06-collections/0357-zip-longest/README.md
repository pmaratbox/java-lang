# 0357 — Zip Longest

Zip [1,2,3] with ["a","b"], filling the missing slot with "-", printing `1a 2b 3-`. Iterate to the longer `Math.max` length, substituting "-" when an index falls outside the shorter `List`.

## Run

    javac ZipLongest.java && java ZipLongest
