# 0087 — Invert a Map

Invert the map `{a: 1, b: 2, c: 3}` (swap keys and values) and print the result sorted by the new key: `1:a 2:b 3:c`. Each value is put as a key into a `TreeMap`, which swaps the pairs and orders them by the new key.

## Run

    javac InvertMap.java && java InvertMap
