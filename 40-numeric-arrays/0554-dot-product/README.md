# 0554 — Dot product

Uses the EJML numeric library's `SimpleMatrix.dot` to compute the dot product of the row vectors `[1,2,3]` and `[4,5,6]`. The library multiplies matching elements and sums them; the result is rounded to an integer for byte-identical output.

## Run

    jbang DotProduct.java
