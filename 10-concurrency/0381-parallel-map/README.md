# 0381 — Parallel Map

Square 1,2,3,4 in parallel and collect the results in input order, printing `1 4 9 16`. Java's ordered parallel stream maps concurrently yet preserves encounter order on collection.

## Run

    javac ParallelMap.java && java ParallelMap
