# 0379 — Future Chain

Chain asynchronous steps 5 -> (*2) -> (+1) and print the final value `11`. Java's `CompletableFuture` composes each step with `thenApply`.

## Run

    javac FutureChain.java && java FutureChain
