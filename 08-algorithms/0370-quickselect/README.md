# 0370 — Quickselect

Find the 3rd smallest element of [7,10,4,3,20,15] with quickselect, printing `7`. Java uses Lomuto partition and recurses into only one side via a loop.

## Run

    javac Quickselect.java && java Quickselect
