# 0189 — Permutations

Generate all permutations of [1,2,3] in lexicographic order, one per line space-separated. Recursive backtracking rotates each candidate into place to preserve order.

## Run

    javac Permutations.java && java Permutations
