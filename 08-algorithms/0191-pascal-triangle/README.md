# 0191 — Pascal Triangle

Print the first 4 rows of Pascal’s triangle, each row space-separated. Each new row is built by summing adjacent entries of the previous int array.

## Run

    javac Triangle.java && java Triangle
