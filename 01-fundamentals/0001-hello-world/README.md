# 0001 — Hello, world!

The minimal Java program: a public class with a `main` method that prints
via `System.out`. The file name must match the public class name.

## Run

    javac HelloWorld.java && java HelloWorld
