# 0134 — FizzBuzz

Print 1..15, replacing multiples of 3 with Fizz, of 5 with Buzz, and of both with FizzBuzz, one per line. In Java, check the `% 15` case first so it takes priority over the individual divisors.

## Run

    javac FizzBuzz.java && java FizzBuzz
