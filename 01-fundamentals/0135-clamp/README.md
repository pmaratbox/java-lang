# 0135 — Clamp a Value

Clamp 15 and -3 into the range [0, 10], printing `10 0`. Java's `Math.max`/`Math.min` compose cleanly into a one-line clamp helper.

## Run

    javac Clamp.java && java Clamp
