# 0333 — Classify Even/Odd

Classify each of [1,2,3,4] as odd or even, printing `odd even odd even`. In Java a ternary on `n % 2 == 0` picks each label cleanly.

## Run

    javac EvenOddClassify.java && java EvenOddClassify
