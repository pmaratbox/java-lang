# 0336 — Swap Two Values

Swap a=1 and b=2 (without bitwise tricks) and print `2 1`. Java has no tuple assignment, so an explicit temporary holds the value during the swap.

## Run

    javac SwapValues.java && java SwapValues
