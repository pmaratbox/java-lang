# 0335 — Iterative Factorial

Compute 5! iteratively, printing `120`. A simple Java `for` loop accumulates the product into a `long`.

## Run

    javac FactorialIterative.java && java FactorialIterative
