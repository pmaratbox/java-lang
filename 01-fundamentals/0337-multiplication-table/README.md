# 0337 — Multiplication Table

Print the 3x3 multiplication table, each row space-separated. Nested Java `for` loops build each row with a `StringBuilder`.

## Run

    javac MultiplicationTable.java && java MultiplicationTable
