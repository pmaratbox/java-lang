# 0094 — Linear Search

Linearly scan the array `4, 2, 7, 1, 9` for `7` and print the index where it is found: `found 7 at index 2`. A loop scans the array until it finds the target; it works on unsorted data, unlike binary search.

## Run

    javac LinearSearch.java && java LinearSearch
