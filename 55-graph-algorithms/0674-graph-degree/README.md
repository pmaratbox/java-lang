# 0674 — Node degree

Build the fixed weighted undirected graph G with Java's `JGraphT` library (`SimpleWeightedGraph`), then ask the graph for the degree of node `b` using `degreeOf`. Node `b` connects to `a`, `c`, and `d`, so the library reports a degree of `3`.

## Run

    jbang GraphDegree.java
