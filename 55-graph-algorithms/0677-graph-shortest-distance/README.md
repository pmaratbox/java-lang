# 0677 — Shortest distance

Java's `JGraphT` library builds the weighted undirected graph G and computes the weighted shortest-path distance from `a` to `e` with `DijkstraShortestPath`. The unique shortest path `a-b-c-d-e` has total weight `4`.

## Run

    jbang GraphShortestDistance.java
