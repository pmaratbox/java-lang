# 0676 — Edge existence

JGraphT models the fixed weighted undirected graph G with `SimpleWeightedGraph`. Its `containsEdge(u, v)` method queries the graph's adjacency structure to report whether an edge connects two vertices — here `b-c` exists while `a-e` does not.

## Run

    jbang GraphHasEdge.java
