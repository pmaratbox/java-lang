# 0673 — Build a graph

This lesson builds the fixed weighted undirected graph G with Java's `JGraphT` library (`SimpleWeightedGraph`), then reports its size by querying the library's own `vertexSet()` and `edgeSet()` — printing the node count and edge count space-joined.

## Run

    jbang GraphBuild.java
