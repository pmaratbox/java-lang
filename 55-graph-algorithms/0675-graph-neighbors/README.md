# 0675 — Neighbors

Build the fixed weighted undirected graph G with JGraphT (`SimpleWeightedGraph`) and list the neighbors of node `a`. `Graphs.neighborListOf` returns the adjacent vertices straight from the library's adjacency structure; we sort them for determinism and comma-join to get `b,c`.

## Run

    jbang GraphNeighbors.java
