# 0079 — Character Codes

Print the character code of `A` and the character for code `66`: `65 B`. `(int) 'A'` widens the `char` to its code; `(char) 66` narrows the `int` back to a character.

## Run

    javac CharacterCodes.java && java CharacterCodes
