# 0354 — Snake to Camel Case

Convert "hello_world" to camelCase `helloWorld`. Java's String.split on underscores plus Character.toUpperCase capitalizes each subsequent word.

## Run

    javac SnakeToCamel.java && java SnakeToCamel
