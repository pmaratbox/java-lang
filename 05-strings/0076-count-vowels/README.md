# 0076 — Count Vowels

Count the vowels (`a`, `e`, `i`, `o`, `u`) in the word `hello` and print the total: `2`. `vowels.indexOf(ch)` returns `-1` when absent, so `>= 0` means the character is a vowel.

## Run

    javac CountVowels.java && java CountVowels
