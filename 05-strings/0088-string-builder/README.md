# 0088 — String Builder

Use a string builder to assemble `1`, `2`, and `3` joined by `-`, printing `1-2-3`. `StringBuilder` is the mutable builder; chained `append` calls avoid creating a new string each time.

## Run

    javac StringBuilderDemo.java && java StringBuilderDemo
