# 0077 — Title Case

Convert `hello world` to title case (uppercase the first letter of each word) and print it: `Hello World`. Each split word's first character is upper-cased with `Character.toUpperCase` and joined with its substring tail.

## Run

    javac TitleCase.java && java TitleCase
