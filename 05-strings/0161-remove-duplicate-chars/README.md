# 0161 — Remove Duplicate Characters

Remove duplicate characters from "aabbcc" keeping first occurrence, printing `abc`. A `LinkedHashSet` tracks seen chars while `add` returning true preserves first-seen order.

## Run

    javac DuplicateChars.java && java DuplicateChars
