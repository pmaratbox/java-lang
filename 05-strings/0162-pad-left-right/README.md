# 0162 — Pad Left and Right

Left-pad and right-pad "5" with spaces to width 3 and print both joined by a pipe: `  5|5  `. `String.format` with `%3s` right-aligns and `%-3s` left-aligns to a fixed width.

## Run

    javac LeftRight.java && java LeftRight
