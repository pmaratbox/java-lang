# 0160 — Most Frequent Character

Find the most frequent character in "hello", printing `l`. Tally into a `LinkedHashMap` with `merge`, then scan for the max count.

## Run

    javac FrequentChar.java && java FrequentChar
