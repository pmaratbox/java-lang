# 0072 — Palindrome Check

Check whether each of `level` and `hello` reads the same forwards and backwards, printing `level: yes` and `hello: no`. `new StringBuilder(s).reverse()` builds the reversed string, which `equals` compares to the original.

## Run

    javac Palindrome.java && java Palindrome
