# 0078 — Substring Search

Find the index of the first occurrence of `lo` in `hello` and print it: `3`. `String.indexOf` returns the index of the first occurrence, or `-1` when the substring is not present.

## Run

    javac SubstringSearch.java && java SubstringSearch
