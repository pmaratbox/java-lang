# 0157 — Longest Common Prefix

Find the longest common prefix of ["flower","flow","flight"], printing `fl`. Shrink a candidate prefix via `substring` as each string is compared character-by-character.

## Run

    javac CommonPrefix.java && java CommonPrefix
