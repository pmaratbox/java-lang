# 0159 — String Interpolation

Interpolate name="Ada" and age=36 into a sentence, printing `Ada is 36`. Java has no string interpolation, so use `String.format` with `%s`/`%d`.

## Run

    javac Interpolation.java && java Interpolation
