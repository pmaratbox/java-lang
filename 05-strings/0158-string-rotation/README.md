# 0158 — String Rotation Check

Check whether "cdab" is a rotation of "abcd", printing `yes`. A rotation holds iff the lengths match and `(a+a).contains(b)`.

## Run

    javac Rotation.java && java Rotation
