# 0355 — Expand Character Range

Expand the range spec "a-e" into the characters `abcde`. A simple char loop from start to end inclusive builds the string.

## Run

    javac ExpandCharRange.java && java ExpandCharRange
