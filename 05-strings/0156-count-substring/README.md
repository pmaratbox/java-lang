# 0156 — Count Substring Occurrences

Count non-overlapping occurrences of "ab" in "ababab", printing `3`. Use `indexOf(t, i)` and advance past each match.

## Run

    javac Substring.java && java Substring
