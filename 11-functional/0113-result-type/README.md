# 0113 — Result / Either Type

Model success and failure with a Result type: safeDiv(10,2) prints `ok: 5` and safeDiv(1,0) prints `err: divide by zero`. Return Ok(value)/Err(msg) (or language equivalent) and pattern-match to print each on its own line.

## Run

    javac Type.java && java Type
