# 0386 — Iterate and Take

Iterate f(x)=x*3 from 1 and take the first four values, printing `1 3 9 27`. Java's `Stream.iterate` applies the function repeatedly, limited to four values.

## Run

    javac IterateTake.java && java IterateTake
