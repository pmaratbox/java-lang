# 0115 — Dispatch Table

Store functions in a map keyed by name, then apply "add" and "mul" to (3,4), printing `7 12`. {"add": +, "mul": *}; look up and call; print both results on one line.

## Run

    javac Table.java && java Table
