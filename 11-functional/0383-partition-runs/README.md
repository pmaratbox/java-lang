# 0383 — Partition Into Runs

Partition [1,1,2,3,3,3] into runs of equal adjacent elements, printing `1 1|2|3 3 3`. Java builds the runs with a simple loop and joins via Stream collectors.

## Run

    javac PartitionRuns.java && java PartitionRuns
