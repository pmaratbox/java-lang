# 0261 — Rotate Left (8-bit)

Rotate the 8-bit value 1 left by 1 (->2) and 128 left by 1 (->1), printing `2 1`. Masking with `& 0xff` keeps the rotation within 8 bits in Java.

## Run

    javac RotateLeft.java && java RotateLeft
