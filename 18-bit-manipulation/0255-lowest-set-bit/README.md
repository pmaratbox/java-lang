# 0255 — Lowest Set Bit

Isolate the lowest set bit of 12 (1100), printing `4`. Java's two's-complement `int` makes `x & (-x)` isolate the lowest set bit directly.

## Run

    javac LowestSetBit.java && java LowestSetBit
