# 0262 — Parity Check

Compute the bit parity (XOR of all bits) of 7 (->1) and 5 (->0), printing `1 0`. Java's `Integer.bitCount(n) & 1` yields the parity directly.

## Run

    javac ParityCheck.java && java ParityCheck
