# 0252 — Reverse Bits in a Byte

Reverse the 8 bits of the value 1 (00000001) to get 128 (10000000), printing `128`. A simple shift-in loop over the low 8 bits reverses them in idiomatic Java.

## Run

    javac ReverseBitsByte.java && java ReverseBitsByte
