# 0253 — Power of Two Check

Test whether 16 (yes) and 18 (no) are powers of two, printing `yes no`. The classic `n > 0 && (n & (n - 1)) == 0` trick works directly on Java `int`.

## Run

    javac PowerOfTwoCheck.java && java PowerOfTwoCheck
