# 0254 — XOR Swap

Swap the values 3 and 5 using only XOR (no temporary), printing `5 3`. Java's compound `^=` operator makes the three-step XOR swap concise.

## Run

    javac SwapXor.java && java SwapXor
