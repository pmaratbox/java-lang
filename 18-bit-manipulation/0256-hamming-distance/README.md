# 0256 — Hamming Distance

Compute the bit Hamming distance between 1 and 4, printing `2`. Java's built-in `Integer.bitCount` gives the popcount of the XOR.

## Run

    javac HammingDistance.java && java HammingDistance
