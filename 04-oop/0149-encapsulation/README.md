# 0149 — Encapsulation

Keep an account balance private, deposit 50 onto an initial 100 through a method, and print `150`. In Java a `private` field is shielded behind a `deposit()` mutator and a getter so callers never touch the balance directly.

## Run

    javac Encapsulation.java && java Encapsulation
