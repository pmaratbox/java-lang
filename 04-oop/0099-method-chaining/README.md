# 0099 — Method Chaining

Build a calculator that supports method chaining — start at `5`, then `.add(3).multiply(2)` — and print the result: `16`. Each mutating method returns `this`, the standard fluent-interface pattern; `result` ends the chain.

## Run

    javac MethodChaining.java && java MethodChaining
