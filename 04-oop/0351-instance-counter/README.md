# 0351 — Instance Counter

Track how many instances of a class have been created; after building three, print `3`. A `static` field shared across all instances is incremented in the constructor.

## Run

    javac InstanceCounter.java && java InstanceCounter
