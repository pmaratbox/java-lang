# 0100 — Static Methods

Define a static (class-level) method `square` that needs no instance, call it as `square(6)`, and print `36`. A `static` method belongs to the class `MathUtil` and is called as `MathUtil.square(...)` without constructing an instance.

## Run

    javac StaticMethods.java && java StaticMethods
