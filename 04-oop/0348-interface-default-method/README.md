# 0348 — Interface Default Method

Define an interface with a default greet() returning "hi" and an implementer that overrides it to "hey", printing `hi hey`. Java interfaces may carry `default` methods that implementers inherit or override.

## Run

    javac InterfaceDefaultMethod.java && java InterfaceDefaultMethod
