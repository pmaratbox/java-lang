# 0152 — toString Override

Override the string conversion of a Point(1,2) so it prints `Point(1, 2)`. Annotating `toString()` with `@Override` lets `println` print the object in the custom format automatically.

## Run

    javac Override.java && java Override
