# 0153 — Comparable Sort

Sort people [(alice,30),(bob,25)] by age ascending and print their names `bob alice`. Implementing `Comparable<Person>` with `compareTo` on age lets `Collections.sort` order them naturally.

## Run

    javac Sort.java && java Sort
