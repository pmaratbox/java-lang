# 0349 — Trait Composition

Compose two capabilities (A printing "a", B printing "b") into one type and invoke both, printing `a b`. Java composes capabilities by implementing multiple interfaces with default methods.

## Run

    javac TraitComposition.java && java TraitComposition
