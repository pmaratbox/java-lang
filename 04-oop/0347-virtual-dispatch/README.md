# 0347 — Virtual Dispatch

Store a Circle, Square, and Triangle in one Shape collection and call describe() on each, printing `circle square triangle`. Java resolves each describe() call at runtime via the object's actual class.

## Run

    javac VirtualDispatch.java && java VirtualDispatch
