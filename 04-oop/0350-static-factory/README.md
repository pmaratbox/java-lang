# 0350 — Static Factory Method

Use a static factory Color.fromHex("#ff0000") to build a color and print its components `255 0 0`. A private constructor plus a `static` factory keeps construction logic in one named method.

## Run

    javac StaticFactory.java && java StaticFactory
