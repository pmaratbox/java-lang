# 0401 — Create an Observable

Build a push-based Observable from scratch that emits 1, 2, 3 to its observer and then completes. A functional-interface `Observable` whose `subscribe` pushes values into an `Observer` record of `next`/`complete` callbacks.

## Run

    javac ObservableCreate.java && java ObservableCreate
