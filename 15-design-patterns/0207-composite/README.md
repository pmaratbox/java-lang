# 0207 — Composite

Sum a composite tree of leaf values 1, 2, 3 through a uniform size() interface, printing `6`. Leaf and Group share a Node interface so size() recurses uniformly over the tree.

## Run

    javac Composite.java && java Composite
