# 0212 — Template Method

Run an algorithm skeleton whose middle step is overridden, printing `start work end` on one line. The abstract base fixes run()'s shape while the subclass supplies the abstract step().

## Run

    javac TemplateMethod.java && java TemplateMethod
