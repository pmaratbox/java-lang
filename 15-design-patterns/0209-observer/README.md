# 0209 — Observer

Notify two observers of a new value 5; each prints its id and the value on its own line. The Subject keeps a list of Watcher observers and dispatches update() to each in registration order.

## Run

    javac Observer.java && java Observer
