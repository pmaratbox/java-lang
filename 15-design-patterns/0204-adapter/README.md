# 0204 — Adapter

Adapt a Celsius source (100) to a Fahrenheit interface, printing `212`. An adapter class wraps the Celsius object and implements the Fahrenheit interface via the conversion formula.

## Run

    javac Adapter.java && java Adapter
