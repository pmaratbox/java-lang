# 0201 — Factory Method

Use a factory method to create shapes by name and print their kinds `circle square`. A static factory method maps the name to a class implementing a common Shape interface.

## Run

    javac FactoryMethod.java && java FactoryMethod
