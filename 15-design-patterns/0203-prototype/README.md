# 0203 — Prototype (Clone)

Clone a prototype object holding 1, mutate the clone to 2, and show the original is unchanged, printing `1 2`. A copy() method deep-copies the field so mutating the clone leaves the original intact.

## Run

    javac Prototype.java && java Prototype
