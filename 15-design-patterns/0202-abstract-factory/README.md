# 0202 — Abstract Factory

Use a "dark" theme abstract factory to build a button and a checkbox, printing `dark-button dark-checkbox`. A ThemeFactory interface yields matching themed widgets implemented as nested classes.

## Run

    javac AbstractFactory.java && java AbstractFactory
