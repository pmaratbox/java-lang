# 0219 — Bridge

Bridge a Circle abstraction to a Red color implementation, printing `red circle`. The Shape abstraction holds a Color implementor, decoupling shape from how its color is realized.

## Run

    javac Bridge.java && java Bridge
