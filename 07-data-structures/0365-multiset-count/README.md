# 0365 — Multiset Counts

Add 1,1,2 to a multiset; print the count of 1 (2), remove one 1, then print the count of 1 (1), as `2 1`. A `HashMap` keyed by element with `merge` to increment models a multiset's counts.

## Run

    javac MultisetCount.java && java MultisetCount
