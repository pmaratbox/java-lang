# 0363 — Circular Queue

In a capacity-3 circular queue enqueue 1,2,3, dequeue once, enqueue 4, then print the contents `2 3 4`. A fixed `int[]` with a head index and size wraps via modulo arithmetic.

## Run

    javac CircularQueue.java && java CircularQueue
