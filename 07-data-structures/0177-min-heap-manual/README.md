# 0177 — Min Heap (Manual)

Implement a binary min-heap by hand, push 3,1,2, pop all, printing `1 2 3`. An `ArrayList` backs the complete binary tree, with sift-up on push and sift-down on pop.

## Run

    javac HeapManual.java && java HeapManual
