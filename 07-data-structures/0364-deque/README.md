# 0364 — Double-Ended Queue

Using a deque, push 1 and 2 to the back and 0 to the front, then print front-to-back `0 1 2`. Java's `ArrayDeque` offers `addFirst`/`addLast` and iterates front-to-back.

## Run

    javac Deque.java && java Deque
