# 0343 — Optional Chaining

Read a deeply optional value a.b.c when present (5) and when absent (default 0), printing `5 0`. Java's Optional.flatMap/map chains the accesses with orElse supplying the fallback.

## Run

    javac OptionalChaining.java && java OptionalChaining
