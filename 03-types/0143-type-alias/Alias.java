public class Alias {
    public static void main(String[] args) {
        // Java has no real type aliases; a wrapper type gives a semantic name.
        int distance = 5; // Meters
        System.out.println("distance: " + distance);
    }
}
