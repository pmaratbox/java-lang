# 0143 — Type Alias

Define a semantic alias for an integer type (e.g. Meters), assign 5, and print `distance: 5`. Java has no type aliases, so a named local or wrapper class carries the intent.

## Run

    javac Alias.java && java Alias
