# 0145 — Nested Struct

Model a person whose address is a nested struct, and print the city `London`. Java records compose naturally for nested value structures.

## Run

    javac Struct.java && java Struct
