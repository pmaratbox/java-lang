# 0342 — Tagged Union Eval

Model an expression tagged union Num | Add and evaluate Add(Num 1, Num 2), printing `3`. Java sealed interfaces plus records and switch pattern matching express the sum type idiomatically.

## Run

    javac TaggedUnionEval.java && java TaggedUnionEval
