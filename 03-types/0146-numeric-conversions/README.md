# 0146 — Numeric Conversions

Truncate 3.9 to the int 3 and widen the int 3 to the float 3.0, printing `3 3.0`. Java uses explicit casts: `(int)` truncates toward zero and `(float)` widens.

## Run

    javac Conversions.java && java Conversions
