# 0344 — Enum Ordinal

For the enum Direction(N,E,S,W) print the ordinal of S (2) and the name at ordinal 3 (W), printing `2 W`. Java enums expose ordinal() and values() reflecting declaration order.

## Run

    javac EnumOrdinal.java && java EnumOrdinal
