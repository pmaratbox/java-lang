# 0345 — Struct Defaults

Create a Point with default fields (0,0), then one overriding x to 5, printing each on its own line `0 0` then `5 0`. A record with a defaults() factory and a withX copy method models defaulted fields in Java.

## Run

    javac StructDefaults.java && java StructDefaults
