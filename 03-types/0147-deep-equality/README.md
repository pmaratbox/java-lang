# 0147 — Deep Equality

Compare two nested structures with equal contents for structural equality and print `equal: yes`. Java records derive a deep, component-wise `equals` automatically.

## Run

    javac Equality.java && java Equality
