# 0390 — Big-Endian Bytes

Encode the integer 258 as two big-endian bytes (1, 2), decode them back to 258, printing `1 2 258`. In Java, use bit shifts and masks to split and recombine the value.

## Run

    javac BinaryEndian.java && java BinaryEndian
