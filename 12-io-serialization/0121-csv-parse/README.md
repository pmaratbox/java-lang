# 0121 — Parse CSV

Parse the two CSV rows `alice,30` and `bob,25` into name=value pairs and print `alice=30 bob=25`. `String.split(",")` slices each row on its comma.

## Run

    javac Parse.java && java Parse
