# 0119 — Read File Lines

Write three lines to a file, read them back, and print `lines: 3`. Java's `Files.readAllLines` reads the whole file into a `List<String>`.

## Run

    javac FileLines.java && java FileLines
