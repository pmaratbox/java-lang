# 0120 — Append to a File

Write "a" to a file, append "b", then read both lines back and print `a b`. The `StandardOpenOption.APPEND` flag opens an existing file for appending.

## Run

    javac ToFile.java && java ToFile
