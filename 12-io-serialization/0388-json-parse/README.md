# 0388 — JSON Parse

Parse the JSON object `{"x":1,"y":2}` and print its entries `x=1 y=2`. In Java, split on delimiters and strip quotes for a flat numeric object.

## Run

    javac JsonParse.java && java JsonParse
