# 0239 — Reduce a Fraction

Reduce the fraction 6/8 to lowest terms `3/4`. Java divides both terms by their gcd computed with a small recursive helper.

## Run

    javac FractionReduce.java && java FractionReduce
