# 0243 — Newton Square Root

Approximate sqrt(2) with Newton iteration and print it to 4 decimals `1.4142`. Java iterates the update and formats the result with `printf("%.4f")`.

## Run

    javac NewtonSqrt.java && java NewtonSqrt
