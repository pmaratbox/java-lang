# 0238 — Fraction Addition

Add the fractions 1/2 and 1/3 and print the reduced result `5/6`. Java cross-multiplies onto a common denominator then divides through by the gcd.

## Run

    javac FractionAdd.java && java FractionAdd
