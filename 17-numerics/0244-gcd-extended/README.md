# 0244 — Extended Euclid

Compute extended gcd(30,12) returning g and Bezout coefficients, printing `6 1 -2` (since 30*1+12*(-2)=6). Java returns the triple as a small long array from the recursion.

## Run

    javac GcdExtended.java && java GcdExtended
